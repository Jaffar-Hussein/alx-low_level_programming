#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - finds length of string
 * @str: string parameter
 * Return: string length
 */
int _strlen(char *str)
{
return (strlen(str));
}
